export function lienket(height: number, width:number, color:string){
    var height: number = height || 50;
    var width: number = width || 100;
    var color: string = color || 'red';
    alert(height);
    alert(width);
    alert(color);
}
